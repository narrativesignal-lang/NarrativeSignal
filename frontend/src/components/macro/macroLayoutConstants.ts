/**
 * Single source of truth for Macro Data (Narrative Radar) layout.
 * Used by MacroLayout so News list height is identical for all categories.
 */

/** Fixed height of the News list panel. Same for General, Stock, Futures, Crypto, and any Custom. */
export const NEWS_LIST_PANEL_HEIGHT = "46vh";

/** Min height of the heatmap block (always visible below the news list). */
export const HEATMAP_MIN_HEIGHT = "220px";
